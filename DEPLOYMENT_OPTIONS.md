# 部署方案选择指南

## 当前情况

- **域名**: www.bizops.top
- **80端口**: 已有服务在运行
- **Investment服务**: 运行在8082端口
- **ECS IP**: 47.102.107.172

## 部署方案对比

### 方案1: 使用路径前缀 `/investment/` ⭐ 推荐

**访问地址**: `https://www.bizops.top/investment/`

**优点**:
- ✅ 不影响现有服务
- ✅ 不需要额外配置DNS
- ✅ 可以共用SSL证书
- ✅ 配置简单

**缺点**:
- ⚠️ 需要修改Next.js的basePath配置
- ⚠️ URL路径较长

**实施步骤**:
1. 修改Next.js配置，添加basePath
2. 在现有Nginx配置中添加location规则
3. 重启Nginx

---

### 方案2: 使用子域名 `investment.bizops.top` ⭐⭐⭐ 最佳

**访问地址**: `https://investment.bizops.top/`

**优点**:
- ✅ 完全独立，不影响现有服务
- ✅ URL简洁美观
- ✅ 便于管理和扩展
- ✅ 符合最佳实践

**缺点**:
- ⚠️ 需要配置DNS解析
- ⚠️ 需要SSL证书包含子域名（或使用通配符证书）

**实施步骤**:
1. 在DNS中添加A记录: `investment.bizops.top -> 47.102.107.172`
2. 配置独立的Nginx server块
3. 配置SSL证书（Let's Encrypt支持通配符）

---

### 方案3: 直接使用端口访问

**访问地址**: 
- `http://47.102.107.172:8082/`
- `https://www.bizops.top:8082/`（如果配置了SSL）

**优点**:
- ✅ 最简单，不需要Nginx配置
- ✅ 完全不影响现有服务
- ✅ 部署最快

**缺点**:
- ⚠️ URL包含端口号，不够美观
- ⚠️ 需要开放8082端口
- ⚠️ 如果使用HTTPS，需要配置端口SSL

**实施步骤**:
1. 确保阿里云安全组开放8082端口
2. 直接运行Docker容器
3. 无需Nginx配置

---

### 方案4: 修改现有Nginx配置（不推荐）

**访问地址**: `https://www.bizops.top/investment/`

**优点**:
- ✅ 共用域名和证书

**缺点**:
- ⚠️ 需要修改现有配置，有风险
- ⚠️ 可能影响现有服务
- ⚠️ 需要仔细测试

---

## 推荐方案

### 🥇 首选：方案2 - 子域名

如果可能，建议使用子域名 `investment.bizops.top`，这是最干净、最专业的方案。

### 🥈 次选：方案1 - 路径前缀

如果暂时无法配置子域名，使用路径前缀 `/investment/` 是最安全的选择。

### 🥉 备选：方案3 - 直接端口

如果只是临时部署或测试，可以直接使用端口访问。

---

## 各方案的具体配置

### 方案1配置（路径前缀）

需要修改Next.js配置：

```javascript
// next.config.js
const nextConfig = {
  reactStrictMode: true,
  output: 'standalone',
  basePath: '/investment',  // 添加这行
  assetPrefix: '/investment',  // 添加这行
}
```

Nginx配置（添加到现有server块）：

```nginx
location /investment/ {
    proxy_pass http://127.0.0.1:8082/;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    rewrite ^/investment/(.*)$ /$1 break;
}
```

### 方案2配置（子域名）

DNS配置：
```
类型: A
主机记录: investment
记录值: 47.102.107.172
TTL: 600
```

Nginx配置（独立server块）：
```nginx
server {
    listen 443 ssl http2;
    server_name investment.bizops.top;
    
    # SSL证书配置
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    location / {
        proxy_pass http://127.0.0.1:8082;
        # ... 其他代理配置
    }
}
```

### 方案3配置（直接端口）

无需Nginx配置，只需：
1. 运行Docker容器
2. 确保安全组开放8082端口

---

## 我的建议

考虑到您的情况，我建议：

1. **短期方案**：使用方案3（直接端口），快速部署测试
   - 访问: `http://47.102.107.172:8082`
   - 或 `https://www.bizops.top:8082`（如果配置了SSL）

2. **长期方案**：使用方案2（子域名），专业且独立
   - 配置DNS: `investment.bizops.top`
   - 配置独立的Nginx server块
   - 使用Let's Encrypt申请SSL证书

3. **如果必须共用域名**：使用方案1（路径前缀）
   - 需要修改Next.js配置添加basePath
   - 在现有Nginx配置中添加location规则

您希望使用哪个方案？我可以根据您的选择提供详细的配置步骤。

